#include <cstdint>

// Returns a with the last cnt bits reversed
uint32_t reverse_bits(const uint32_t a, const uint32_t cnt);

// Returns smallest power of two >= x
uint32_t bit_ceil(const uint32_t x);
